(()=>{const host=document.querySelector('#adult-soccer-rosters');if(!host)return;const TEAMS=['Team Red','Team Blue','Team Green','Team Gold'];const cls=t=>String(t||'').toLowerCase().replace('team ','team-').replace(/[^a-z-]/g,'');const esc=v=>String(v??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));function render(rosters){const by=new Map((rosters||[]).map(r=>[r.team,Array.isArray(r.players)?r.players:[]]));host.innerHTML=`<div class="adult-rosters__grid">${TEAMS.map(team=>{const players=by.get(team)||[];return `<article class="adult-roster ${cls(team)}"><h3>${esc(team.replace('Team ',''))} <span class="adult-roster__count">${players.length} player${players.length===1?'':'s'}</span></h3>${players.length?`<div class="adult-roster__names">${players.map(name=>`<span class="adult-roster__name">${esc(name)}</span>`).join('')}</div>`:'<div class="adult-roster__empty">No registered players</div>'}</article>`}).join('')}</div>`}async function load(){host.innerHTML='<div class="adult-rosters__message">Loading registered Adult Soccer teams…</div>';try{const r=await fetch('/api/team-event-rosters?eventKey=adult-soccer',{cache:'no-store'}),d=await r.json();if(!r.ok||d.error)throw new Error(d.error||'Could not load team rosters.');render(d.rosters)}catch(e){host.innerHTML=`<div class="adult-rosters__message error">${esc(e.message||'Could not load team rosters.')}</div>`}}load()})();