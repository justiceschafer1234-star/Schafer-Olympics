const adminStatusControl=document.querySelector('#admin-status');const clearResultsButton=document.querySelector('#clear-results');resultRecorded=function(row){return row?.properties?.Status==='Complete'};
function selectedAdminRow(){return latestRows.find(item=>item.id===adminEvent.value)}
function syncAdminStatus(){const row=selectedAdminRow();adminStatusControl.value=row?.properties?.Status||'Not Started'}
function syncBronzeAvailability(){const row=selectedAdminRow();const isTwoTeam=Number(row?.properties?.['Number of teams'])===2;[adminBronze1,adminBronze2].forEach(select=>{select.disabled=isTwoTeam;if(isTwoTeam)select.value=''});const labels=[adminBronze1,adminBronze2].map(select=>select.closest('.field')).filter(Boolean);labels.forEach(label=>{label.style.opacity=isTwoTeam?'0.45':'1';label.title=isTwoTeam?'Bronze is not used for two-team events.':''});if(row&&isTwoTeam){const p=row.properties||{};const parts=[`Event ${p['Event #']??'—'}: ${p.Event||'Untitled event'}`,'2-team event: Gold and Silver only — Bronze disabled'];if(p['🥇 Gold Points']!=null)parts.push(`Gold = ${fmt(p['🥇 Gold Points'])} pts`);if(p['🥈 Silver Points']!=null)parts.push(`Silver = ${fmt(p['🥈 Silver Points'])} pts`);adminPreview.textContent=parts.join(' · ')}}
const originalRenderAdmin=renderAdmin;renderAdmin=function(rows){originalRenderAdmin(rows);syncAdminStatus();syncBronzeAvailability()};adminEvent.addEventListener('change',()=>{syncAdminStatus();setTimeout(syncBronzeAvailability,0)});
scoreForm.addEventListener('submit',async function(event){event.preventDefault();event.stopImmediatePropagation();adminMessage.className='admin-message';adminMessage.textContent='';const row=selectedAdminRow();const isTwoTeam=Number(row?.properties?.['Number of teams'])===2;const bronzeTeams=isTwoTeam?[]:[adminBronze1.value,adminBronze2.value].filter(Boolean),placements=[adminGold.value,adminSilver.value,...bronzeTeams].filter(Boolean);if(!adminEvent.value||!adminGold.value||!adminSilver.value){adminMessage.classList.add('error-text');adminMessage.textContent='Choose the event, gold, and silver.';return}if(new Set(placements).size!==placements.length){adminMessage.classList.add('error-text');adminMessage.textContent='Each team can only appear once.';return}saveScore.disabled=true;try{const response=await fetch('/api/admin/scores',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({code:adminCode.value,eventId:adminEvent.value,goldTeam:adminGold.value,silverTeam:adminSilver.value,bronzeTeams,status:adminStatusControl.value})}),data=await response.json();if(!response.ok||!data.ok)throw new Error([data.error,data.notionMessage].filter(Boolean).join(' ')||'Could not save result.');adminMessage.classList.add('success-text');adminMessage.textContent='✓ Saved. Updating scoreboard…';await loadScores();syncAdminStatus();syncBronzeAvailability();adminMessage.textContent='✓ Result and status updated.'}catch(error){adminMessage.classList.add('error-text');adminMessage.textContent=error?.message||'Could not save result.'}finally{saveScore.disabled=false}},true);
clearResultsButton.addEventListener('click',async()=>{const row=selectedAdminRow();if(!row){adminMessage.className='admin-message error-text';adminMessage.textContent='Choose an event first.';return}if(!adminCode.value){adminMessage.className='admin-message error-text';adminMessage.textContent='Enter your private score-entry code first.';return}const name=row.properties?.Event||'this event';if(!confirm(`Clear ALL results for “${name}”?\n\nThis removes all medal placements and their points.`))return;if(!confirm(`FINAL CONFIRMATION\n\nAre you absolutely sure you want to clear “${name}”? The event will be reset to Not Started.`))return;clearResultsButton.disabled=true;adminMessage.className='admin-message';adminMessage.textContent='Clearing results…';try{const response=await fetch('/api/admin/scores',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'clear',code:adminCode.value,eventId:adminEvent.value})}),data=await response.json();if(!response.ok||!data.ok)throw new Error([data.error,data.notionMessage].filter(Boolean).join(' ')||'Could not clear results.');adminMessage.className='admin-message success-text';adminMessage.textContent='✓ Results cleared. Event reset to Not Started.';await loadScores();adminEvent.value=row.id;fillAdminFromSelectedEvent();syncAdminStatus();syncBronzeAvailability()}catch(error){adminMessage.className='admin-message error-text';adminMessage.textContent=error?.message||'Could not clear results.'}finally{clearResultsButton.disabled=false}});