const adminStatusControl=document.querySelector('#admin-status');const clearResultsButton=document.querySelector('#clear-results');resultRecorded=function(row){return row?.properties?.Status==='Complete'};
const asTeams=value=>(Array.isArray(value)?value:value?[value]:[]).filter(Boolean);const selectedTeams=select=>Array.from(select.selectedOptions).map(o=>o.value).filter(Boolean);const setSelectedTeams=(select,values)=>{const wanted=new Set(asTeams(values));Array.from(select.options).forEach(o=>o.selected=wanted.has(o.value))};
adminGold.multiple=true;adminSilver.multiple=true;adminGold.size=4;adminSilver.size=4;adminGold.removeAttribute('required');adminSilver.removeAttribute('required');adminGold.closest('.field')?.querySelector('span')?.append(' — select 1 or 2');adminSilver.closest('.field')?.querySelector('span')?.append(' — select 1 or 2');
function selectedAdminRow(){return latestRows.find(item=>item.id===adminEvent.value)}function syncAdminStatus(){const row=selectedAdminRow();adminStatusControl.value=row?.properties?.Status||'Not Started'}
function syncBronzeAvailability(){const row=selectedAdminRow();const isTwoTeam=Number(row?.properties?.['Number of teams'])===2;[adminBronze1,adminBronze2].forEach(select=>{select.disabled=isTwoTeam;if(isTwoTeam)select.value=''});[adminBronze1,adminBronze2].map(select=>select.closest('.field')).filter(Boolean).forEach(label=>{label.style.opacity=isTwoTeam?'0.45':'1';label.title=isTwoTeam?'Bronze is not used for two-team events.':''});if(row&&isTwoTeam){const p=row.properties||{};const parts=[`Event ${p['Event #']??'—'}: ${p.Event||'Untitled event'}`,'Combined event: select exactly 2 Gold teams + 2 Silver teams — Bronze disabled'];if(p['🥇 Gold Points']!=null)parts.push(`Gold = ${fmt(p['🥇 Gold Points'])} pts each`);if(p['🥈 Silver Points']!=null)parts.push(`Silver = ${fmt(p['🥈 Silver Points'])} pts each`);adminPreview.textContent=parts.join(' · ')}}
fillAdminFromSelectedEvent=function(){const row=selectedAdminRow();if(!row){setSelectedTeams(adminGold,[]);setSelectedTeams(adminSilver,[]);adminBronze1.value='';adminBronze2.value='';adminPreview.textContent='Pick an event to enter or edit its result.';return}const p=row.properties||{},bronze=asTeams(p['🥉 Team']);setSelectedTeams(adminGold,p['🥇 Team']);setSelectedTeams(adminSilver,p['🥈 Team']);adminBronze1.value=bronze[0]||'';adminBronze2.value=bronze[1]||'';const parts=[`Event ${p['Event #']??'—'}: ${p.Event||'Untitled event'}`];if(p['🥇 Gold Points']!=null)parts.push(`Gold = ${fmt(p['🥇 Gold Points'])} pts`);if(p['🥈 Silver Points']!=null)parts.push(`Silver = ${fmt(p['🥈 Silver Points'])} pts`);if(p['🥉 Bronze Points']!=null)parts.push(`Bronze = ${fmt(p['🥉 Bronze Points'])} pts`);adminPreview.textContent=parts.join(' · ');syncBronzeAvailability()};
medalCounts=function(rows){const counts=Object.fromEntries(TEAMS.map(team=>[team,{gold:0,silver:0,bronze:0}]));rows.forEach(row=>{const p=row.properties||{};asTeams(p['🥇 Team']).forEach(team=>{if(counts[team])counts[team].gold++});asTeams(p['🥈 Team']).forEach(team=>{if(counts[team])counts[team].silver++});asTeams(p['🥉 Team']).forEach(team=>{if(counts[team])counts[team].bronze++})});return counts};
podiumText=function(p){const bits=[],gold=asTeams(p['🥇 Team']),silver=asTeams(p['🥈 Team']),bronze=asTeams(p['🥉 Team']);if(gold.length)bits.push(`🥇 ${gold.map(x=>esc(x.replace('Team ',''))).join(' + ')}`);if(silver.length)bits.push(`🥈 ${silver.map(x=>esc(x.replace('Team ',''))).join(' + ')}`);if(bronze.length)bits.push(`🥉 ${bronze.map(x=>esc(x.replace('Team ',''))).join(' + ')}`);return bits.join('<span>•</span>')};
const originalRenderAdmin=renderAdmin;renderAdmin=function(rows){originalRenderAdmin(rows);adminGold.multiple=true;adminSilver.multiple=true;adminGold.size=4;adminSilver.size=4;fillAdminFromSelectedEvent();syncAdminStatus();syncBronzeAvailability()};adminEvent.addEventListener('change',()=>{setTimeout(()=>{fillAdminFromSelectedEvent();syncAdminStatus();syncBronzeAvailability()},0)});
scoreForm.addEventListener('submit',async function(event){event.preventDefault();event.stopImmediatePropagation();adminMessage.className='admin-message';adminMessage.textContent='';const row=selectedAdminRow(),isTwoTeam=Number(row?.properties?.['Number of teams'])===2,goldTeams=selectedTeams(adminGold),silverTeams=selectedTeams(adminSilver),bronzeTeams=isTwoTeam?[]:[adminBronze1.value,adminBronze2.value].filter(Boolean),placements=[...goldTeams,...silverTeams,...bronzeTeams];if(!adminEvent.value||!goldTeams.length||!silverTeams.length){adminMessage.classList.add('error-text');adminMessage.textContent='Choose the event, gold, and silver.';return}if(isTwoTeam&&(goldTeams.length!==2||silverTeams.length!==2)){adminMessage.classList.add('error-text');adminMessage.textContent='This 2-team event needs exactly 2 Gold teams and 2 Silver teams.';return}if(!isTwoTeam&&(goldTeams.length!==1||silverTeams.length!==1)){adminMessage.classList.add('error-text');adminMessage.textContent='This event uses 1 Gold team and 1 Silver team.';return}if(new Set(placements).size!==placements.length){adminMessage.classList.add('error-text');adminMessage.textContent='Each team can only appear once.';return}saveScore.disabled=true;try{const response=await fetch('/api/admin/scores',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({code:adminCode.value,eventId:adminEvent.value,goldTeams,silverTeams,bronzeTeams,status:adminStatusControl.value,isTwoTeam})}),data=await response.json();if(!response.ok||!data.ok)throw new Error([data.error,data.notionMessage].filter(Boolean).join(' ')||'Could not save result.');adminMessage.classList.add('success-text');adminMessage.textContent='✓ Saved. Updating scoreboard…';await loadScores();adminEvent.value=row.id;fillAdminFromSelectedEvent();syncAdminStatus();adminMessage.textContent='✓ Result and status updated.'}catch(error){adminMessage.classList.add('error-text');adminMessage.textContent=error?.message||'Could not save result.'}finally{saveScore.disabled=false}},true);
clearResultsButton.addEventListener('click',async()=>{const row=selectedAdminRow();if(!row){adminMessage.className='admin-message error-text';adminMessage.textContent='Choose an event first.';return}if(!adminCode.value){adminMessage.className='admin-message error-text';adminMessage.textContent='Enter your private score-entry code first.';return}const name=row.properties?.Event||'this event';if(!confirm(`Clear ALL results for “${name}”?\n\nThis removes all medal placements and their points.`))return;if(!confirm(`FINAL CONFIRMATION\n\nAre you absolutely sure you want to clear “${name}”? The event will be reset to Not Started.`))return;clearResultsButton.disabled=true;adminMessage.className='admin-message';adminMessage.textContent='Clearing results…';try{const response=await fetch('/api/admin/scores',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({action:'clear',code:adminCode.value,eventId:adminEvent.value})}),data=await response.json();if(!response.ok||!data.ok)throw new Error([data.error,data.notionMessage].filter(Boolean).join(' ')||'Could not clear results.');adminMessage.className='admin-message success-text';adminMessage.textContent='✓ Results cleared. Event reset to Not Started.';await loadScores();adminEvent.value=row.id;fillAdminFromSelectedEvent();syncAdminStatus()}catch(error){adminMessage.className='admin-message error-text';adminMessage.textContent=error?.message||'Could not clear results.'}finally{clearResultsButton.disabled=false}});