(()=>{const participant=document.querySelector('#registration-participant'),eventsBox=document.querySelector('#registration-events'),identity=document.querySelector('#registration-identity'),save=document.querySelector('#save-registration'),message=document.querySelector('#registration-message');if(!participant)return;let data=null,current=null;const esc=s=>String(s??'').replace(/[&<>"']/g,c=>({'&':'&amp;','<':'&lt;','>':'&gt;','"':'&quot;',"'":'&#39;'}[c]));const labels={Man:'Men',Woman:'Women',Kid:'Kids'};function eligible(p,e){const pd=p.divisions||[],ed=e.divisions||[];return pd.some(d=>ed.includes(labels[d]));}function render(){current=data.participants.find(p=>p.id===participant.value)||null;if(!current){identity.hidden=true;eventsBox.innerHTML='<p class="panel-note">Choose your name to load eligible events.</p>';save.disabled=true;return}const list=data.events.filter(e=>eligible(current,e));identity.hidden=false;identity.innerHTML=`<strong>${esc(current.name)}</strong>`;eventsBox.innerHTML=list.length?list.map(e=>`<label class="registration-event"><input type="checkbox" value="${esc(e.id)}" ${current.registeredEventIds.includes(e.id)?'checked':''}><span><strong>#${e.number??'–'} ${esc(e.name)}</strong><small>${esc(e.format||'Event')}</small></span></label>`).join(''):'<p class="panel-note">No eligible events were found.</p>';save.disabled=!list.length;message.textContent='';}async function load(){try{const r=await fetch('/api/registration');data=await r.json();if(!r.ok)throw new Error(data.error||'Unable to load registration.');participant.innerHTML='<option value="">Choose your name…</option>'+data.participants.map(p=>`<option value="${esc(p.id)}">${esc(p.name)}</option>`).join('');}catch(e){eventsBox.innerHTML=`<p class="setup-message">${esc(e.message)}</p>`;}}participant.addEventListener('change',render);save.addEventListener('click',async()=>{if(!current)return;save.disabled=true;message.textContent='Saving…';const eventIds=[...eventsBox.querySelectorAll('input:checked')].map(x=>x.value);try{const r=await fetch('/api/registration',{method:'POST',headers:{'Content-Type':'application/json'},body:JSON.stringify({participantId:current.id,eventIds})}),d=await r.json();if(!r.ok)throw new Error(d.error||'Unable to save registration.');current.registeredEventIds=eventIds;message.textContent='✓ Registration saved';}catch(e){message.textContent=e.message}finally{save.disabled=false}});load();})();